"""Wide completion menu plugin.

Stretches the slash-command completion menu to the full terminal width so the
scrollbar pins to the right edge of the screen instead of floating at the
right edge of the longest completion.
"""
