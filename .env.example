# Fid Coder Configuration
# Copy this file to .env if you want local overrides.
# The .env file takes priority over ~/.fid_coder/fid.cfg
#
# Fid Coder uses GitHub Copilot only — no API keys required.
# Sign in from the app with:
#   /copilot-login
